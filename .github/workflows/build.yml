name: Build AccSaberQuest

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: List repo files
        run: find . -not -path './.git/*' -type f

      - name: Set up Android NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r26d
          add-to-path: true

      - name: Install QPM
        run: |
          curl -SLo /tmp/qpm.zip "https://github.com/QuestPackageManager/QPM.CLI/releases/download/v1.5.9/qpm-linux-x64.zip"
          unzip /tmp/qpm.zip -d /tmp/qpm_extracted
          QPM_BIN=$(find /tmp/qpm_extracted -type f | head -1)
          cp "$QPM_BIN" /tmp/qpm
          chmod +x /tmp/qpm
          /tmp/qpm --version

      - name: QPM restore
        run: |
          /tmp/qpm restore || true
          mkdir -p extern/includes extern/libs
          QPM_CACHE=~/.local/share/QPM-RS/cache
          find $QPM_CACHE -path "*/shared/*" -name "*.hpp" | while read f; do
            pkg=$(echo $f | cut -d'/' -f9)
            mkdir -p extern/includes/$pkg
            cp "$f" extern/includes/$pkg/ 2>/dev/null || true
          done
          find $QPM_CACHE -name "lib*.so" ! -name "*.debug.so" -exec cp {} extern/libs/ \; 2>/dev/null || true
          cat > extern/extern.cmake << 'EOF'
          set(EXTERN_DIR ${CMAKE_CURRENT_LIST_DIR}/extern)
          include_directories(${EXTERN_DIR}/includes)
          link_directories(${EXTERN_DIR}/libs)
          EOF

      - name: Dump all header locations
        run: |
          QPM_CACHE=~/.local/share/QPM-RS/cache
          echo "=== beatsaber-hook ==="
          find $QPM_CACHE/beatsaber-hook -name "*.hpp" | head -5
          echo "=== bs-cordl ==="
          find $QPM_CACHE/bs-cordl -name "*.hpp" | head -5
          echo "=== bsml ==="
          find $QPM_CACHE/bsml -name "*.hpp" | head -5
          echo "=== custom-types ==="
          find $QPM_CACHE/custom-types -name "*.hpp" | head -5
          echo "=== libil2cpp ==="
          find $QPM_CACHE/libil2cpp -type f | head -10
          echo "=== scotland2 ==="
          find $QPM_CACHE/scotland2 -name "*.hpp" | head -5
          echo "=== paper2 ==="
          find $QPM_CACHE/paper2_scotland2 -name "*.hpp" | head -5

      - name: Install Ninja
        run: sudo apt-get install -y ninja-build

      - name: Configure CMake
        run: |
          rm -rf build
          cmake -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_TOOLCHAIN_FILE=${{ steps.setup-ndk.outputs.ndk-path }}/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-32 \
            -DANDROID_STL=c++_static

      - name: Build
        run: cmake --build build --config Release

      - name: Locate QMOD
        id: locate
        run: |
          QMOD=$(find build -name "*.qmod" | head -1)
          echo "qmod_path=$QMOD" >> $GITHUB_OUTPUT

      - name: Upload QMOD artifact
        uses: actions/upload-artifact@v4
        with:
          name: AccSaberQuest
          path: ${{ steps.locate.outputs.qmod_path }}
          if-no-files-found: error
